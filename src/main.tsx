import { createRoot } from 'react-dom/client'
import App from './App.tsx'
import './index.css'

// React rendering disabled to allow static HTML in index.html
// createRoot(document.getElementById("root")!).render(<App />);

